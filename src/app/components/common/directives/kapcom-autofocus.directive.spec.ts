import { KapcomAutofocusDirective } from './kapcom-autofocus.directive';

describe('KapcomAutofocusDirective', () => {
  it('should create an instance', () => {
    const directive = new KapcomAutofocusDirective();
    expect(directive).toBeTruthy();
  });
});
